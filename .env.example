# OBS WebSocket connection settings
# Copy this file to .env and edit the values to match your OBS WebSocket configuration.
OBS_HOST=localhost
OBS_PORT=4455
OBS_PASSWORD=change_me

# Optional: override default config.yaml path
# RECORDING_AUTO_CONFIG=./config.yaml

# Optional: local API server bind address and port
# API_HOST=127.0.0.1
# API_PORT=8765
